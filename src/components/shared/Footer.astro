---
const year = new Date().getFullYear();
---

<footer class="relative w-full bg-gradient-to-r from-pink-600 via-rose-500 to-fuchsia-700 text-white py-10 mt-20 animate-gradient-x overflow-hidden">
  <div class="max-w-4xl mx-auto text-center px-6 relative z-20">
    <h2 class="text-2xl md:text-3xl font-dancing drop-shadow-lg">
      🌹 Kat & Tú 🌹
    </h2>
    <p class="mt-2 opacity-90 text-sm md:text-base">
      Siempre unidos, más allá del tiempo ✨
    </p>

    <p class="mt-6 text-xs opacity-75">
      © {year} Kat & Tú — Amor eterno 💕
    </p>
  </div>

  <!-- Corazones fijos con efecto de latido (sin desplazarse con scroll) -->
  <div class="absolute inset-0 pointer-events-none z-10 flex justify-center gap-8 opacity-30">
    <span class="text-3xl animate-pulse">💖</span>
    <span class="text-3xl animate-pulse delay-200">💕</span>
    <span class="text-3xl animate-pulse delay-500">💘</span>
  </div>

  <style>
    @keyframes gradient-x {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }
    .animate-gradient-x {
      background-size: 200% 200%;
      animation: gradient-x 18s ease infinite;
    }
    .delay-200 { animation-delay: 0.2s; }
    .delay-500 { animation-delay: 0.5s; }
  </style>
</footer>

